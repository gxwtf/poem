export const metadata = {
    title: "句读知不知 - 广学古诗文",
    description: "句读知不知小游戏",
}

export default function Layout({
    children,
}: {
    children: React.ReactNode
}) {
    return <>{children}</>
}